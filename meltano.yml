send_anonymous_usage_stats: true
project_id: "tap-cmic"
default_environment: test
requires_meltano: ">=4.0"
environments:
- name: test
plugins:
  extractors:
  - name: "tap-cmic"
    namespace: "tap_cmic"
    pip_url: -e .
    capabilities:
    - state
    - catalog
    - discover
    - about
    - stream-maps
    - structured-logging

    # TODO: Declare settings and their types here:
    settings:
    - name: username
      label: Username
      description: The CMiC API service account username

    - name: password
      label: Password
      description: The CMiC password
      sensitive: true

    - name: start_date
      kind: date_iso8601
      label: Start Date
      description: Initial date to start extracting data from

    - name: base_url
      label: Base URL
      description: CMiC Basic Auth API base URL, without a trailing slash

    # TODO: Declare required settings here:
    settings_group_validation:
    - [base_url, username, password]

    # TODO: Declare default configuration values here:
    config:
      start_date: 6 months ago
      base_url: https://atlas-api.cmiccloud.com/cmicprod

  loaders:
  - name: target-jsonl
    variant: andyh1203
    pip_url: target-jsonl
